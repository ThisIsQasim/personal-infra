---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: samba
  namespace: nas
  labels:
    app: samba
spec:
  replicas: 1
  selector:
    matchLabels:
      app: samba
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: samba
    spec:
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
      - image: servercontainers/samba:smbd-only-latest
        name: samba
        env:
        - name: MODEL
          value: "Macmini"
        - name: SAMBA_CONF_ENABLE_NTLM_AUTH
          value: "y"
        - name: ACCOUNT_qasim
          valueFrom:
            secretKeyRef:
              name: samba-password
              key: qasim
        - name: ACCOUNT_media
          valueFrom:
            secretKeyRef:
              name: samba-password
              key: media
        - name: UID_qasim
          value: "1001"
        - name: UID_media
          value: "1000"
        - name: GROUPS_media
          value: "qasim"
        - name: SAMBA_VOLUME_CONFIG_qasimonly
          value: "[Qasim's Files]; path=/shares/qasim; valid users = qasim; guest ok = no; read only = no; browseable = yes; writable = yes"
        - name: SAMBA_VOLUME_CONFIG_media
          value: "[Media]; path=/shares/media; valid users = media; guest ok = no; read only = yes; browseable = yes; writable = no"
        - name: AVAHI_DISABLE
          value: "true"
        - name: WSDD2_DISABLE
          value: "true"
        volumeMounts:
        - mountPath: /shares/qasim
          name: external
        - mountPath: /shares/media
          name: media
        ports:
        - containerPort: 445
        - containerPort: 139
        livenessProbe:
          tcpSocket:
            port: 445
          initialDelaySeconds: 30
          periodSeconds: 5
        readinessProbe:
          tcpSocket:
            port: 445
          initialDelaySeconds: 15
          periodSeconds: 5
        resources:
          requests:
            cpu: "100m"
            memory: "512Mi"
          limits:
            cpu: "3000m"
            memory: "5120Mi"
      volumes:
        - name: external
          hostPath:
            path: /mnt/external
            type: Directory
        - name: media
          hostPath:
            path: /mnt/external/media
            type: Directory
